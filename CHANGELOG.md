# Changelog

本文件记录对外可见的行为变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### Added

- `doctor <device_id>` 通过只读 `term --list-sessions` 探测指定设备的终端通道，不创建、终止或抢占会话。
- 终端诊断会区分通道未激活/版本不兼容、锁屏、会话占用、设备不可用和旧版 CLI，并输出下一步建议。

### Changed

- `TermBridge` 的启动失败、断开和超时错误复用同一诊断分类，提示先在 UU 远程 GUI 打开终端等修复动作。

## [1.1.0] - 2026-09-19

协议层修复：把「靠不越界碰巧正确」改成「要么返回正确数据，要么显式报错」。全部结论来自 UU远程 **4.41.0.2311** 真机复测（Windows → Windows），证据见 `PROTOCOL.md` 与 `TEST-REPORT.md` 复测节。

### Fixed

- **静默丢行（高危）**：`exec` 输出行超过 **120 列**时，屏幕按 120 列折行，一条输出占 2 个屏行；旧实现按「数组元素」分页，30 行宽输出只返回 4 行且退出码为 0。现按**屏行成本**自适应分页，并用会话级 token 重组折行。
- **静默返回错误数据（高危）**：`read` 在 ≥8KB 时可能返回变长数据（实测 32752/32761/32765/32781B，源 32768B，退出码 0）。根因是 `UU_FLEN` 长度标记未渲染时**校验被整体跳过**；现改为**校验缺失即失败**，并校验解码后字节长度。
- **屏幕模型不是视口模型（高危根因）**：`VtScreen` 无 120 列折行、无 39 行视口、无滚动逐出，导致宽行内容被拼进同一行（实测单行 4450 字符）与跨页残留。现实现完整视口语义（折行 + 滚动逐出 + 脏行跟踪）。
- **写入假失败**：`write` 超时预算 `max(30s, len/2)` 低于实测吞吐（~1.7KB/s），32KB 写入实际成功却报超时、64KB 超时后远端文件状态未定义。现预算改为 `max(60s, 1.5s/KB)`，超时消息明确「远端状态未知」并给出 `Get-FileHash` 核对命令。
- **读不存在的文件可能死等**：MISS 标记未渲染时会一直等到内部超时（实测 120s+）。现远端显式上报状态标记，单轮等待预算 45s 后提前重试，实测 **5s** 返回明确错误。
- **读目录返回「成功 + 空输出」**：`ISDIR`/`TOOBIG` 分支被前置 `break` 短路（死代码）。现远端上报 `UU_ST=<OK|MISS|ISDIR|TOOBIG>`，客户端优先判定状态。
- **超时错误吞掉真实错因**：锁屏时 CLI 输出「检测到被控端已锁屏…」后取空密码退出，旧实现只报「命令超时」。现超时错误带上 stderr 诊断（过滤 `[连接]` 类进度噪声，保留错误关键词行）。
- **`platform` 字段裸值**：`list`/`doctor` 直接打印官方返回的数字（实测 `platform=1`）。现经 `platformName()` 归一为 `Windows`。

### Changed

- `exec`/`read` 分页：对端每页回报「元素数 + 屏行数」，解析数不符则**换新哨兵**重拉（旧代码复用旧哨兵会假成功），3 次仍不符即报错。
- 分页命令长度受控：计数逻辑改为握手期定义的远端函数 `uuPg`（命令过长会在回显处折行，折行碎片会被误当输出）。
- `read` 安全包络（实测）：≤1KB 与 8KB–64KB 均字节级一致；更大文件仍建议走 UU远程 GUI 文件传输。
- 文档：`SKILL.md` 明确 **term 仅支持 Windows 被控端**（官方明文），zsh/bash 分支标注为未验证实验代码；补充「实测安全包络与校验语义」一节；`scripts/uu-push-file.ps1` 标注**不可用**（上游脚本在当前版本标记渲染丢失）。
- `SKILL.md` frontmatter：移除规范外字段，改用 `license` + `metadata`；description 划清与官方 `uuyc-cli` skill 的边界（设备/云电脑/协助管理归官方 skill）。
- 新增离线回归：`node tools/protocol-regress.cjs` 重放 `tests/fixtures/*.raw`（11 个真机原始字节 fixture，已脱敏），**不触远程**。

### Known limitations

- **宽行折行的行内字节损坏（未修）**：服务端 ECH 擦除与内容重写非原子，偶发单行内 1–2 个字符被空格替换（4 次宽行测试命中 1 次 1 行）。行边界可靠，**字节级完整性需对关键数据用 `NAME=VALUE` + 哈希自行核对**。
- 仅验证 Windows → Windows；macOS 被控端官方未支持。
- 被控端锁屏时 CLI 管道通道不可用（需先解锁）。
- 通道劣化窗口（例如外部强杀进程后遗留的僵尸会话）下分页会失败——**一律显式报错**，不会静默返回残缺数据；此时用 `sessions` + `kill` 清理后重试。

## [1.0.0] - 2026-09-10

首个生产可用版本：`exec` / `read` / `write` / `pty` / `sessions` / `kill` / `doctor`，38 项真机验收（详见 `TEST-REPORT.md`）。
