# PROTOCOL.md — uuyc-cli `term` 管道通道服务端行为实测常量

> 定位：本项目全部健壮性都建立在**逆推**的服务端渲染/握手行为上。本文件把这些经验值升级为**带原始字节证据、带版本号、可复验**的常量表。
> 逆推不是缺陷，是本项目的立身之本；抗风险策略是「**能检测就大声失败**」，不是「不再逆推」。
>
> 实测环境：主控端 UU远程 **4.41.0.2311**（CLI 同版本）｜ 被控端 `<device>`（Windows）｜ Windows → Windows ｜ 2026-09-19
> 取证工具：`tools/protocol-probe.ts` → `tools/protocol-probe.cjs`（命令：`npx esbuild tools/protocol-probe.ts --bundle --platform=node --outfile=tools/protocol-probe.cjs`）
> 回归 fixtures（**已入库、已脱敏**）：`tests/fixtures/*.raw` —— 纯 stdout 字节流，可离线重放（`node tools/protocol-regress.cjs`）。
> 本轮完整证据归档：`vendor/wip/p1-evidence/`（本地保留，`vendor/` 不进 Git，clone 后不存在）

---

## 1. 屏幕几何：**39 行 × 120 列**

### 1.1 行数 = 39（二分实测）

方法：`Clear-Host; 1..k | %{ 'M' + $_.ToString('0000') }; Write-Output 'SENT'`，逐步加大 k，看最早的行何时滚出屏幕。

| k | 屏上 marker 数 | M0001 是否还在 | 推算（k + 2 行命令回显 vs 容量） |
|---|---|---|---|
| 24 | 24 | 在 | 26 |
| 32 | 32 | 在 | 34 |
| 35 | 35 | 在 | 37 |
| 36 | 36 | 在 | 38 |
| **37** | **37** | **在** | **39 ≤ 容量** |
| **38** | 37 | **丢** | **40 > 容量** |
| 39 | 37 | 丢 | 41 − 39 = 2 丢 |
| 40 | 37 | 丢 | 42 − 39 = 3 丢 |
| 48 | 37 | 丢 | 50 − 39 = 11 丢 |

**结论：服务端虚拟屏容量 = 39 行。**（证据：`rows-2026-09-19T10-10-50.raw`、`rows-2026-09-19T10-11-08.raw`）

> 文档纠错：`src/vt.ts:5` 注释「24 行的虚拟屏幕」**错误**（上游 VSCode 项目残留，且与同文件下方「超长行不折行」注释自相矛盾）；`src/termBridge.ts:14` 的「约 39 行」**正确**。

### 1.2 列数 = 120

- 原始流中出现 `ESC[8;30;120t`（终端尺寸协商，证据：所有 `rows-*.raw` 首段）
- 单行总宽 **118 列** → 单屏行；**121 列** → 折行
- ECH（`CSI X`）最大运行长度实测 **85**，恰好等于 `205 − 120`（宽行折行后剩余的尾段长度）

**结论：屏幕宽 120 列；行长 > 120 列会折行为 2 行。**

---

## 2. 折行的后果：**`exec` 静默丢数据**（本次最高价值发现）

`pullPages()` 按 `PAGE_ROWS = 26` 取「数组元素行」，但**一个元素可能占 2 个屏幕行**；更根本的是客户端屏幕模型不是视口模型（见 §11）。

`bin/uu-bridge.cjs exec` 实测（30 行输出，每行 `W<nn>` + N 个 `x`）：

| 单行总宽 | 返回行数 | 判定 |
|---|---|---|
| 63 列 | **30/30** | ✅ 安全 |
| 103 列 | **30/30** | ✅ 安全 |
| **121 列** | **4/30**（缺 W01–W26） | ❌ 静默丢 26 行，`exit=0`，**无任何报错** |
| 125 列 | **23/30**（缺 11 行） | ❌ 静默丢 11 行 |
| 203 列 | **4/30**（两次独立运行完全一致） | ❌ 100% 可复现 |

**分页时序取证**（`tools/protocol-probe.cjs pages`，精确复刻 assign + pullPages）：

| 负载 | page 0..25（期望 26） | page 26..29（期望 4） |
|---|---|---|
| 30 行 × 短行（对照） | `screenRows=29`，markers=**26**，`maxRowLen=17` | markers=**4** |
| 30 行 × 203 列 | `screenRows=39`，markers=**18**，`maxRowLen=4450` | markers=**21**（含 page 1 残留 P10–P26） |

**判定**：`exec` 路径在**输出行宽 > 120 列**时不可信，且是**静默**的（退出码 0）。`read` 不受影响（base64 76 列 < 120，且有长度+形状双校验）—— 这正是「校验」存在的价值反证。

**根因见 §11**（不是单纯的服务端丢数据，而是客户端屏幕模型缺陷叠加服务端视口行为）。

**建议修复方向**（本次不实施，属修订计划）：
1. **主修**：把 `src/vt.ts` 改成真视口模型——120 列折行、39 行视口、滚动逐出、脏行跟踪（见 §11）
2. **兜底**：分页按「屏行」而非数组元素计（`ceil(len/120)` 折算），或对端回报每页非空行数并在不符时重拉
3. **强约束**：无论哪种，都必须「**检测到不确定就显式报错**」，禁止静默返回残缺结果

---

## 3. 冲刷语义：`Clear-Host` vs 60 空行

| 方式 | 线上字节形态 | 短行 k=60 存活 |
|---|---|---|
| `Clear-Host` | 恰好 **1 次** `ESC[2J`（全屏清） | 37（= 容量上限） |
| `Write-Output ("`n" * 60)` | **60 × (`ESC[K` + CRLF)**，无 `2J` | 37（= 容量上限） |

**结论**：在健康窗口下两者**结果等价**（都是容量上限 37）。F-3 记载的「60 空行触发差分渲染器丢宽行」在本次暖态窗口**未复现**；`Clear-Host` 的价值（全屏重绘、每行完整下发）**需要一个不稳定窗口才能判定**，暂记为「未复现，非否证」。

> 修正我此前的判断：宽行「丢失」的主因是**折行 × 39 行容量**（§2），不是差分渲染器丢内容。

---

## 4. ECH（`CSI X`）与「行内容被擦」

- 实测 ECH 调用 **31 次 / 共擦除 2053 字符**，单次最大 **85** 字符（= 折行尾段长度）
- 原始流中 marker 出现 **27 个**，最终屏上只剩 **18 个** → **9 行被 ECH 擦除后没有再补写内容**

**结论**：`ESC[K`/`CSI X` 的擦除与内容重写**不是原子的**；中间态被观察到时表现为「行内容被擦」（TEST-REPORT 记录的 `LINE-105` → `     105`）。这是**服务端渲染竞态**，客户端只能靠「长度/计数校验 + 重拉」缓解，无法根治。

---

## 5. 会话退出会清屏（离线重放必须裁尾）

`.raw` 全量重放 → 屏幕 0 行；裁掉尾部后：

| 裁尾字节 | 屏上行数 | marker 数 |
|---|---|---|
| 0 | 0 | 0 |
| 200 | 20 | 2 |
| **400** | **20** | **18**（= 实时探针值） |
| 800 | 20 | 18 |

尾部形态：`ESC[H` + 连续 `ESC[K` + `ESC]0;…` + `ESC[?25h` + `ESC[?9001l` + `ESC[?1004l`。

**结论**：
1. 退出序列会擦除屏幕 → **任何取证重放必须裁掉尾部 ~300-400B**，否则结论为空
2. 反向印证 `TermBridge.dispose()`「先取输出、后发 `exit`」的顺序是**必需的**（先 exit 就什么都读不到）

---

## 6. 时延与吞吐

| 项 | 实测（暖态，n=5） |
|---|---|
| 握手（spawn → 哨兵可见） | **1.3–2.4 s** |
| 命令 → 哨兵可见 | **604 / 603 / 605 / 965 / 1810 ms** |
| 命令 → 静默确认完成（settle） | **1026 / 1026 / 1148 / 1447 / 2233 ms** |

证据：`rt-2026-09-19T10-10-26.raw` / `.json`（单次 exec 全程 ~7s 的旧基线包含了握手 + settle，与本次一致）

---

## 7. 管道模式的固有副作用

stderr 每次会话固定出现：

```
Warning: Failed to enable VT output for terminal
```

**结论**：stdout 非 TTY 时 CLI 无法启用 VT 输出，但**渲染流仍然照发**（所有实测均在 pipe 下成功）。此行应与 `[连接]`/`[系统]` 一样按噪声过滤。

---

## 8. 被控端锁屏：term 通道同样被拦（原「待验未知项」现已判定）

```
$ uuyc-cli term --device-id <id> --list-sessions
[连接] 初始化连接
[连接] 优化连接路径
[系统] 检测到被控端已锁屏，请输入被控端账户密码验证身份，此过程不会解锁被控端。
[系统] 请输入被控端解锁密码: Error: empty password
exit=6
```

**结论**（推翻官方文档归档中的待验项 1；归档见 `vendor/official-docs/`，本地保留、不进 Git）：
1. 「锁屏需系统账户验证」**同样作用于 CLI `term` 管道通道**，不只是 GUI 终端
2. 无 TTY 时密码取空 → `Error: empty password`，退出码 6 → **锁屏态下非交互自动化直接不可用**
3. 解锁后同一命令立即恢复（exit=0）
4. **新缺陷**：`bin/uu-bridge.cjs sessions` 在这条路径上挂 30s 后只报 `ERROR: 命令超时(30000ms)`，把 `[系统] 检测到被控端已锁屏` 这个真实错因吞掉（F-7 同类，修在 `termBridge` 未修 `src/cli.ts:253 execCliText`）

证据：`G0b-20260919-180743.log`（锁屏态）vs `G0c-20260919-180854.log`（解锁后）

---

## 9. 会话卫生（本轮 30+ 次建会话的实测）

- 30+ 次 `--new-session`（含探针与 bridge `exec`）：**零 2005 限流、零 409**
- 正常路径全部 `exit` 收尾，**零僵尸会话**（`--list-sessions` 仅剩用户自己的 `<user-session>`）
- `sessions` 输出与原生 `term --list-sessions` **逐字节一致**（bridge 额外滤掉 `[连接]` 噪声行）

### 9.1 异常路径会泄漏远程会话（实测）

探针在**超时抛错路径**只 `child.kill()`、未发 `exit` → 实测泄漏 2 个会话（`session2` / `session3`），需 `--kill-session` 手动清理。

- 产品侧 `src/main.ts` 用 `finally { bridge.dispose() }` 避开，**但 `dispose()` 本身也是「发 exit → 等 600ms → 强杀」**：进程卡死时仍会泄漏
- 运维含义：任何出现超时/强杀的运行后，**必须**跑一次 `sessions` 核对（SKILL.md 铁律已有此条，本次给出了确切数据）
- 错误设备 ID 的握手失败耗时实测 **~20s**（CLI 自身超时，非本项目可控）

---

## 10. 已判定 vs 未判定
| 项 | 状态 | 依据 |
|---|---|---|
| 屏幕 39×120、折行阈值 | ✅ 已判定 | §1 |
| 宽行 >120 列 → `exec` 静默丢行 | ✅ 已判定 | §2 |
| Clear-Host vs 60 空行等价性 | ⚠️ 暖态下等价；不稳定窗口未复现 | §3 |
| ECH 擦除竞态 | ✅ 已观察到机制 | §4 |
| 退出清屏 | ✅ 已判定 | §5 |
| 锁屏拦截 term 通道 | ✅ 已判定 | §8 |
| 冷态「主控端版本过低」报文 | ⬜ 待测（需关 GUI 窗口的冷暖实验） | 计划 P1-冷态 |
| 被控端 macOS/zsh 分支 | ⬜ **UNVERIFIED**（无 macOS 被控端；官方亦明文 term 仅支持 Windows 被控端） | — |
| `platform` 取值枚举 | ⬜ **UNVERIFIED**（本账号仅 1 台 Windows 设备，官方文档写字符串、实测返回数字 `1`） | — |

---

## 11. 根因判定：客户端屏幕模型不是视口模型（统一 F-9 / F-12 / F-3-擦除）

`src/vt.ts` 的 `VtScreen` 实现里**没有**：

| 应有 | 实现现状 | 后果 |
|---|---|---|
| 120 列折行 | `writeChar()` 只 `cursorCol++`，无列上限 | 宽行内容被拼进同一「行」，实测 `maxRowLen=4450` |
| 39 行视口 | 无视口常量、无行数上限 | 服务端视口相对坐标被应用到无边界模型 |
| 滚动逐出 | `snapshotLines()` 返回 `1..maxRow` 全部行，从不逐出 | **跨页残留**：page 2 的屏上仍含 page 1 的 P10–P26 |
| 脏行跟踪 | 无 | 无法区分「本命令输出」与「上命令残留」 |

### 为什么现在还能工作

bridge 的实际使用模式恰好避开了全部缺陷：

- 分页 ≤ 26 行 → 屏上行数 ≤ 39，**不触发滚动**
- base64 分页 76 列 → **不触发折行**
- 每次 `send()` 先 `screen.reset()` → 避开残留（但代价是丢掉服务端真实状态）

**即：当前实现是「靠不越界碰巧正确」。越界（宽行折行 / 跨页滚动）即静默出错。**

### 与旧结论的关系

| 旧编号 | 现象 | 本文判定 |
|---|---|---|
| F-9 | 页内丢行（26 → 24） | 同一根因（无折行/残差核算）；否证「纯服务端渲染竞态」作为唯一解释 |
| F-12 | 宽行 → 4/30 静默丢行 | 同一根因；服务端视口丢滚出行的行为叠加模型缺陷 |
| F-3「行内容被擦」 | `LINE-105` → `     105` | ECH 擦除与重写非原子（§4）+ 模型无脏行跟踪，两者叠加 |

### 修复方向（主修 + 兜底，缺一不可）

1. **主修 `src/vt.ts`**：真视口模型（120 列折行 + 39 行滚动逐出 + 脏行跟踪）；`writeChar` 达 120 列时换行，`cursorRow > 39` 时整体上移并丢弃第 1 行。
2. **可离线验证**：`tests/fixtures/*.raw`（已入库、已脱敏）是**原始字节流**，可直接重放作回归 fixtures（含宽行、短行、跨页、退出清屏四类），无需碰远程。
3. **兜底 `pullPages`**：按屏行折算页大小，并在解析行数与对端回报不一致时**显式抛错**（该方向已在本版实现：屏行成本分页 + 换哨兵重拉 + 计数不符报错；历史补丁存档于 `vendor/wip/f9-pages-verify.patch`，不进 Git）。
4. **回归基线**：把本轮 4 个宽度档位（63/103/121/125 列）与 `pages` 对照/实验组固化为验收用例。

---

## 12. 尺寸安全包络与通道劣化（2026-09-19 补测）

### 12.1 `read` / `exec` 的实测可信区间

**修复前（2026-09-19 上午，4.41.0.2311）**

| 负载 | 结果 | 可信 |
|---|---|---|
| 50 B 中文+空行 | 双向字节一致 ×2 | ✅ |
| 1 KB 读回 | 1024B 完全一致 ×2 | ✅ |
| 8 KB 读回 | 8190/8192B 静默少 2B（1/2）；另 1/2 显式超时 | ❌ |
| 32 KB 读回 | 32752 / 32761 / 32765 / 32781B（源 32768B），**exit=0 无报错** ×4 | ❌ |
| `exec` 输出行 ≤120 列 | 30/30 ×2 | ✅ |
| `exec` 输出行 >120 列 | 4/30 ×2（静默） | ❌ |
| 读不存在的文件 | 2/3 正确 MISS；1/3 挂死 150s+ | ⚠️ |
| 读目录 | **rc=0 且空输出**（ISDIR 分支被短路，见 F-22） | ❌ |

机制：`UU_FLEN` 行未被渲染时 `expectLen = -1` → **长度校验被整体跳过**；`count` 亦可被脏屏污染。

**修复后（同日 P7 真机回归，清理劣化会话后）**

| 负载 | 结果 |
|---|---|
| `exec` 63/103/121/125/203 列 × 30 行 | **30/30 全部完整**（7–10s），零静默丢行 |
| `exec` 26/27/52/53/200 行分页 | **全部精确且有序**（6–16s） |
| `write` 1KB/8KB/32KB/64KB | rc=0，远端 `Get-FileHash` **全部与本地吻合**（5/14/43/72s） |
| `read` 1KB/8KB/32KB/64KB | **全部字节级一致**（6/12/160/57s） |
| 读不存在 | rc=1，**5s** 明确 MISS |
| 读目录 | rc=1，**7s** `is a directory` |
| 读 300KB | rc=1，**5s** `file too large: TOOBIG|307200` |
| 写不存在目录 | rc=1，显式 PS 异常消息 |

- 修复后**零静默错误**：期间出现的两次失败（8KB、32KB）都是**显式报错**（`分页校验失败…拒绝返回可能残缺的数据`），清理僵尸会话后重测全部一致
- **残余风险 F-21**：宽行折行时服务端 ECH 擦除与重写非原子，偶发单行内 1–2 个字符被空格替换（4 次宽行测试命中 1 次 1 行）。元素 token 只能保证“行边界”，字节级完整性需对关键数据用 `NAME=VALUE` + 哈希自行核对

### 12.2 写入超时是假失败（F-17）

`writeFile()` 的预算：`timeoutMs = Math.max(30000, content.length / 2)`

| 负载 | 预算 | 实测墙钟 | 远端结果 |
|---|---|---|---|
| 32 KB | 30.0 s | ~34 s | ✅ 完整（SHA256 吻合）但报 `写文件超时` |
| 64 KB | 32.8 s | >32.8 s | ❌ 远端文件不完整（观察到 32789B 残留） |

**结论**：预算低于实测吞吐（~1.7KB/s）所需时间 → 大文件写入**系统性假失败**；且超时后远端文件状态未定义（可能完整、可能残缺），调用方无法区分。

### 12.3 通道劣化窗口（F-20）

健康窗口 vs 劣化窗口的实测差异：

| 指标 | 健康窗口（P1 阶段） | 劣化窗口（P4 之后） |
|---|---|---|
| `exit` 收尾 | ✅ 30+ 次零残留 | ❌ 泄漏约 1 次/调用 |
| 分页读取 | ✅ 26/26 + 4/4 | ❌ `分页读取超时(第 26/104 行起)` |
| 不存在的文件 | 未测 | ⚠️ 1/3 挂死 150s+ |
| `--list-sessions` | 正常 | 正常（**本身不泄漏**） |

**判定**：劣化窗口的存在意味着 §12.1 的文件传输结论**可能部分是窗口相关**；但 F-12（宽行）两次独立运行一致且与 39×120 常量自洽，判为非窗口偶然。

**复验前置**：重启主控端 GUI 终端窗口（重新建立暖通道）或等通道冷却后重跑 §12.1。
